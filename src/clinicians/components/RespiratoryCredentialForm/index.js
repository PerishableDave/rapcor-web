import React, { Component } from 'react'
import { Field, reduxForm } from 'redux-form'

const CredentialForm = props => {
}

export default class RespiratoryCredentialForm extends Component {
  render() {
    return (
      <form onSubmit={handleSubmit} >
      </form>
    )
  }
}
